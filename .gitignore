# Ignore everything
*

# But do NOT ignore these:
!.gitignore
!README.md
!pyproject.toml

# Ignore everything in dosegen
dosegen/*

# Unignore the folder itself
!dosegen/

# Unignore all .py files directly inside dosegen (including __init__.py)
!dosegen/*.py

# Optionally: unignore .py files in subfolders too
!dosegen/**/*.py